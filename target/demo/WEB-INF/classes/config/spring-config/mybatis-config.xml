<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTDConfig 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">

<!-- 配置映射文件   -->
<configuration>
    <mappers>
        <!--映射专业-->
        <mapper resource="department.xml"></mapper>
        <!-- 映射班级 -->
        <mapper resource="classes.xml"></mapper>
        <!-- 映射学生 -->
        <mapper resource="students.xml"></mapper>
    </mappers>
</configuration>